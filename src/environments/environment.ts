import { EnvironmentInterface } from '../app/utilities/interfaces/global.interface';

export const environment: EnvironmentInterface = {
  baseURL: '',
  websiteURL: '',
  production: true,
  version: '',
};